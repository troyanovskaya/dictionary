import { ZoomDDirective } from './zoom-d.directive';

describe('ZoomDDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomDDirective();
    expect(directive).toBeTruthy();
  });
});
