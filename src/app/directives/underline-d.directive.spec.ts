import { UnderlineDDirective } from './underline-d.directive';

describe('UnderlineDDirective', () => {
  it('should create an instance', () => {
    const directive = new UnderlineDDirective();
    expect(directive).toBeTruthy();
  });
});
