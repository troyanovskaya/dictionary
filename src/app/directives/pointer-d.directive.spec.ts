import { PointerDDirective } from './pointer-d.directive';

describe('PointerDDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerDDirective();
    expect(directive).toBeTruthy();
  });
});
