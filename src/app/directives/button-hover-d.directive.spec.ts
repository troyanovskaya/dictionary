import { ButtonHoverDDirective } from './button-hover-d.directive';

describe('ButtonHoverDDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonHoverDDirective();
    expect(directive).toBeTruthy();
  });
});
