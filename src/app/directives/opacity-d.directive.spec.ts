import { OpacityDDirective } from './opacity-d.directive';

describe('OpacityDDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityDDirective();
    expect(directive).toBeTruthy();
  });
});
